{
  "name": "@hris/attendance-db",
  "version": "1.0.0",
  "private": true,
  "description": "Attendance & Timekeeping DB",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.8.2"
  },
  "devDependencies": {
    "prisma": "^6.8.2",
    "typescript": "^5.2.2"
  }
}
